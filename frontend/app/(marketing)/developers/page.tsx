import { MarketingPage } from "@/components/marketing/MarketingPage";
export default function Page() {
  return <MarketingPage title="Developers" subtitle="Start local, wire dependencies, and integrate API-first identity controls quickly." bullets={["OpenAPI-driven endpoints and typed payloads", "Dockerized local stack with OPA and OpenFGA", "Console mock layer for UI-first exploration", "Clear production checklist and deployment assets"]} />;
}
