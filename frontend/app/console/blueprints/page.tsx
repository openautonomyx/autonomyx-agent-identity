import { PlaceholderPage } from "@/components/console/PlaceholderPage";
export default function Page() {
  return <PlaceholderPage title="Blueprints" subtitle="Reusable onboarding templates for metadata, policy defaults, and approval requirements." items={["Template library", "Required approvals by sensitivity tier", "Default capabilities and tags", "Assignment flow during registration"]} />;
}
