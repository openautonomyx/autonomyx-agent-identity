import { PlaceholderPage } from "@/components/console/PlaceholderPage";
export default function Page() {
  return <PlaceholderPage title="Discovery Catalog" subtitle="Discoverable trusted agents and their capabilities by environment and trust profile." items={["Capability/tag filters", "Trust level and policy visibility", "Endpoint and integration metadata", "Owner, tenant, and status facets"]} />;
}
